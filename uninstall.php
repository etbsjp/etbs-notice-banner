<?php
/**
 * アンインストール時の処理。
 *
 * 方針は etbs の配布プラグイン共通の「案A」（task-queue #108）。判定は3分類。
 *
 * | 利用者が作ったコンテンツ | 利用者が設定した値 | 一時状態・自分が仕掛けた cron |
 * |---|---|---|
 * | 消さない | 消さない | 消す |
 *
 * このプラグインでの当てはめ:
 * - 残す … オプション etnb_notice（お知らせの見出し・本文・掲載期間。利用者が書いたコンテンツ）
 * - 残す … オプション etnb_layout（出し方。管理者が設定した値）
 * - 消す … 該当なし。一時データ etnb_form_<ユーザー ID>（入力エラー時に入力値を画面へ戻すためのもの）は
 *           5分で期限が切れて WordPress が捨てるため、ここで探して消す必要が無い。cron も持たない。
 *
 * 消さない理由は害の非対称性。残す害は DB に小さな行が2つ残るだけだが、消す害は復旧できない
 * （入れ直したときに、書いてあったお知らせが消えている）。
 *
 * @package etbs-notice-banner
 */

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}
